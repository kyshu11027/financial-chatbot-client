const constants = {
  SSE_TIMEOUT: 2000,
  END_MESSAGE_STRING: "[DONE]",
  ERROR_MESSAGE_STRING: "[ERROR]",
  MAX_CHAT_INPUT_HEIGHT: 250,
  MAX_SSE_RETRIES: 5,
};

export default Object.freeze(constants);
